# v2.0.0-dev.4 — Full Re-Sync and final History hardening

This development checkpoint starts from integration commit `9852b905860bded1344a0b27a601fada7637c630` after the user-tested PR #15 merge.

## Scope

- Add a separate advanced Full Re-Sync action for an already COMPLETE Hour/Day/Month history baseline.
- Keep normal Sync All unchanged as the routine initial/incremental update path.
- Route Full Re-Sync through the existing validated per-family full traversal and safety shell with `FULL_RESYNC` state-machine semantics.
- Preserve immediate persistence and the existing conflict/revision model; never delete known history before rereading it.
- Remove the temporary History Sync debug card/helper now that the physical v2 family gates are complete.
- Localize the new action and confirmation UI in all six supported languages.
- Advance the side-by-side test build to `2.0.0-dev.4` / versionCode 42.

## Validation boundary

No NFC transport command, parser, family selector, FCB progression, traversal evidence rule or persistence identity is changed. The new action is orchestration/UI around already validated family runners and already modelled `FULL_RESYNC` state semantics.

The dev.4 UI can be visually tested without performing an expensive full archive reread. The final v2 release plan still calls for one limited RC real-device smoke rather than another complete physical archive campaign.
